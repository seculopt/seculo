# Site photography is part of the deployed site and MUST be committed —
# it was previously ignored here, which shipped a live site with no images.
# If these need optimising, resize them in place; do not un-track them.
